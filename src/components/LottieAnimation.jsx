import React from 'react';
import Lottie from 'lottie-react';

// Placeholder for scale-of-justice animation
// In a real implementation, you would import the actual Lottie JSON file
const ScaleOfJusticeAnimation = () => {
  // This is a placeholder - you would need the actual Lottie JSON file
  const defaultOptions = {
    loop: true,
    autoplay: true,
    // animationData: require('./scale-of-justice.json'), // Uncomment when you have the JSON file
  };

  return 
  <div>
    <img src="/images/scale-of-justice.png" alt="Scale of Justice" />
  </div>;
};

export default ScaleOfJusticeAnimation; 